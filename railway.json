{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run install:all && rm -rf client/build client/node_modules/.cache && npm run build && echo 'BUILD_TIMESTAMP=$(date -u +%Y-%m-%dT%H:%M:%SZ)' > .build-info"
  },
  "deploy": {
    "startCommand": "cd server && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "version": "4"
}


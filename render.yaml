services:
  - type: web
    name: talendro-app
    env: node
    buildCommand: npm run install:all && npm run build
    startCommand: cd server && npm install && npm start
    envVars:
      - key: NODE_ENV
        value: production


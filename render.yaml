services:
  - type: web
    name: talendro-app
    env: node
    buildCommand: npm run install:all && rm -rf client/build && npm run build
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production

